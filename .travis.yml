language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install:
  - "pip install numpy --use-mirrors"
# command to run tests
script: "python setup.py install --prefix=../mdp_install && cd ../mdp_install && python -c 'import mdp;mdp.test()'"
